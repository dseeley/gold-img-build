FROM ubuntu:18.04

##Faster version - copies from local instead of downloads.
#COPY {{packerfile_fname}} {{kickstartfile_fname}} id_rsa_shutdown_nopw id_rsa_esxisvc_nopw {{ubuntu_iso.url | basename}} {{packer_pkg_url | basename}} /goldimg/
#RUN cd goldimg \
#  && apt-get update; apt-get -y install unzip \
#  && unzip {{packer_pkg_url | basename}} \
#  && ./packer build ./{{packerfile_fname}}

COPY {{packerfile_fname}} {{kickstartfile_fname}} id_rsa_shutdown_nopw id_rsa_esxisvc_nopw /goldimg/
RUN  cd goldimg \
  && apt-get update; apt-get -y install unzip wget \
  && wget {{ubuntu_iso.url}} \
  && wget {{packer_pkg_url}} \
  && unzip {{packer_pkg_url | basename}} \
  && ./packer build ./{{packerfile_fname}}
